export class ProductImage {
  
  constructor(

    public id?: number,
    public productId?: number,
    public imagePath?: string,
    public thumbnailImage?: boolean,
    public detialImage?: boolean,

    ) { }
}
